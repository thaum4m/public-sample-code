# sample-webpack-ts
Example of using Webpack with TypeScript via ts-loader to build a sample web application.

## Usage
1. To build the application in development mode enter:
```
npm run develop
```

2. View the output in a browser by navigating to *dist/index.html*.
